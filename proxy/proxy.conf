user nginx;
events {
  worker_connections 1000;
}
http {
  server {
    listen 3333;
    location / {
      proxy_redirect off;
      proxy_buffering off;
      proxy_pass http://api:3000;
    }
  }
}